import { StyleSheet } from "react-native";

export const trafficCss = StyleSheet.create({
    light: {
        width: 100,
        height: 100,
        borderRadius: 50,
        marginBottom: 20
    }
});